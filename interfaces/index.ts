export type User = {
  id: number;
  name: string;
};

export type mockUser = {
  name: string;
  email: string;
  list: number;
  country: string;
};

export type Todo = {
  id: number;
  description: string;
  isSuccess: boolean;
};
